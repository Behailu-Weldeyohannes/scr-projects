let person = {
    name: "Tolosa",
    age: 34,
    country: "Eth"
}

function logData(){
    return `${person.name} is ${person.age} years old and lives in ${person.country}`
}
console.log(logData());
